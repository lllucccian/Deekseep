<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dsmod.probe"
    android:versionCode="32"
    android:versionName="1.7.3">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <package android:name="com.deepseek.chat" />
    </queries>

    <application
        android:label="Deekseep"
        android:description="@string/moddesc"
        android:forceQueryable="true"
        android:extractNativeLibs="true"
        android:allowBackup="false">

        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="@string/moddesc" />
        <meta-data android:name="xposedminversion" android:value="82" />
        <meta-data android:name="xposedscope" android:resource="@array/xposedscope" />

        <activity
            android:name="com.dsmod.probe.SettingsActivity"
            android:exported="true"
            android:label="Deekseep"
            android:theme="@android:style/Theme.Material.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.dsmod.probe.PromptPickerActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar" />

        <activity
            android:name="com.dsmod.probe.LocalApiKeepAliveActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="deekseep-module"
                    android:host="local-api-keepalive" />
            </intent-filter>
        </activity>

        <service
            android:name="com.dsmod.probe.LocalApiKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the user-enabled DeepSeek localhost API and SSE stream responsive in the background." />
        </service>

        <!-- Traditional Xposed does not send the modern service Binder, but the same provider
             authenticates target heartbeats and local-API keepalive control in both stable APKs. -->
        <provider
            android:name="com.dsmod.probe.XposedActivationProvider"
            android:authorities="com.dsmod.probe.XposedService"
            android:exported="true"
            android:grantUriPermissions="false" />

        <receiver
            android:name="com.dsmod.probe.XposedActivationReceiver"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name="com.dsmod.probe.ProactiveHeartbeatReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
